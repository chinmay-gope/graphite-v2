/**
 * Graphite exception hierarchy.
 *
 * <p>This package defines the runtime exceptions used throughout the
 * library to report invalid graph operations, algorithm
 * preconditions, configuration errors, and graph validation failures.
 *
 * <p>All Graphite-specific exceptions inherit from
 * {@code GraphException}.
 *
 * @since 2.0
 */

package io.graphite.exception;
